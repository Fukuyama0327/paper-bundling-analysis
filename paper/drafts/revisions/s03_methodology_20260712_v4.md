# Methodology — 下書き（日本語）

> **方針メモ（2026-07-12）**
> - 現行 `main.tex` の「モデル構築」を基礎とし，その後に整理された `notes/closed_form_expected_contracts.md`，`notes/procurement_bundling_issues.md`，`paper/planning/objective_design.md` および調達バンドリング勉強メモを反映する．
> - `references/論文化に向けた論点_追加スライド  -  修復済み.pptx` の期待契約件数・距離制約に関する整理と，`references/インフラ群のマネジメント最適化に向けた，契約発注のバンドリングと広域連携効果の定量分析.pptx` のスライド21・22に示されたマルコフ劣化ハザードモデルおよび期待契約件数の解析的導出を反映する．
> - 数式を示す前に，期待契約件数を評価指標として用いる理由と，その指標が表さない範囲を明示する．
> - 期待契約件数は発注効率全体ではなく，補修需要をより少ない契約単位へ集約できる可能性を表す指標として位置付ける．
> - 閉形式 $f(N,L)$ はシミュレーション結果のフィッティングではなく，独立・同一確率の仮定下で導かれる期待値の解析式として扱う．旧分数関数は本文では用いない．
> - 地域分割モデルは正本コード `scripts/run_gurobi_districting.py` に合わせ，距離上限を超える橋梁ペアの同一地域割当を直接禁止する．現行TeXの補助変数 $y_{ijk}$ は冗長であるため用いない．
> - 本モデルは実際の契約件数を予測するものではなく，管理エリア，距離上限，地域数および同時発注上限の違いを共通の仮定下で比較する意思決定支援モデルとする．

---

## 3.1 モデルの枠組みと評価指標

本研究では，複数自治体が管理する橋梁を行政界にかかわらず複数の管理エリアへ割り当て，各エリアで発生する橋梁補修需要を契約バンドリングによって集約する状況を考える．本研究の目的は，確率的に発生する補修需要について，管理エリアの空間構成が契約バンドリングの機会と年間期待契約件数に与える影響を評価することである．また，過度な広域化を避けるため，同一エリアに含まれる橋梁間の最大距離に上限を設ける．

モデルは，確率的補修需要，契約バンドリングルールおよび地域分割最適化の三つの要素から構成される．はじめに，点検データから推定したマルコフ推移確率行列を用いて，単一橋梁の年間補修需要発生確率を算定する．次に，エリア内で補修対象となる橋梁数の確率分布と，一契約に含められる橋梁数の上限から，エリア内橋梁数に応じた年間期待契約件数を求める．最後に，その期待値を目的関数として，距離制約の下で橋梁の地域割当を最適化する．

期待契約件数は，管理エリアの変更によって補修需要をどの程度少ない契約単位へ集約できるかを表す．したがって，その減少は契約集約機会の増加を意味するが，施工費の削減や規模の経済による単価低下を直接表すものではない．本モデルは個別契約を予測するものではなく，管理エリアの空間構成が契約集約の機会に与える影響を比較するためのモデルである．

## 3.2 確率的補修需要と期待契約件数

### 3.2.1 劣化過程と単一橋梁の補修需要発生確率

橋梁の健全度は，離散的な状態間を時間とともに推移する確率過程として表す．健全度の状態空間を $\{1,\ldots,m\}$ とし，状態番号が大きいほど劣化が進行しているものとする．点検前後の健全度と点検間隔から，津田ら（2005）のマルコフ劣化ハザードモデルを用いて1年間の推移確率行列 $P$ を推定する．

最大劣化状態 $m$ を補修対象状態とし，状態 $m$ に到達した橋梁は補修後に状態1へ回復すると仮定する．補修前の劣化過程を表す推移確率行列 $P$ を，非補修状態間の推移行列 $T$ と，非補修状態から補修状態への推移確率ベクトル $r$ を用いて，

$$
P=
\begin{pmatrix}
T & r\\
0 & 1
\end{pmatrix}
$$

と表す．補修による状態1への回復を組み込むと，補修状態を除いた更新過程の推移確率行列は，

$$
\widetilde{P}=T+re_1^{\mathsf T}
$$

となる．ここで，$e_1=[1,0,\ldots,0]^{\mathsf T}$ は状態1を表す単位ベクトルである．$\widetilde{P}$ の定常分布 $\pi$ は，$\pi\widetilde{P}=\pi$ および $\pi\mathbf{1}=1$ を満たし，

$$
\pi=
\frac{e_1^{\mathsf T}(I-T)^{-1}}
{e_1^{\mathsf T}(I-T)^{-1}\mathbf{1}}
$$

と表される．この定常分布は，補修状態への到達後に状態1へ回復する更新過程において，橋梁が各非補修状態に存在する長期的な割合を表す．したがって，単一橋梁が1年間に補修対象状態へ到達する確率 $q$ は，

$$
q=\pi r
$$

によって与えられる．

### 3.2.2 契約バンドリングルール

地域 $k$ に含まれる橋梁数を $N_k$ とする．各橋梁が同一の年間補修需要発生確率 $q$ を持ち，互いに独立に補修対象になると仮定すると，地域 $k$ において1年間に補修対象となる橋梁数 $X_k$ は，

$$
X_k\sim\mathrm{Binomial}(N_k,q)
$$

に従う．したがって，補修対象橋梁数が $n$ 本となる確率は，

$$
\Pr(X_k=n)
=
\binom{N_k}{n}q^n(1-q)^{N_k-n}
$$

である．

一契約に含められる橋梁数の上限を $L$ とする．同一年度かつ同一地域内で発生した補修需要を，上限 $L$ まで同一契約に含められると仮定すると，補修対象橋梁が $n$ 本の場合に必要となる契約件数 $C(n;L)$ は，

$$
C(n;L)=
\begin{cases}
0, & n=0,\\
\left\lceil n/L\right\rceil, & n\geq 1
\end{cases}
$$

となる．ここで，$\lceil n/L\rceil$ は $n/L$ 以上の最小の整数を表す天井関数である．$L=1$ は各橋梁を個別に発注する場合を表し，$L>1$ は最大 $L$ 橋まで同一契約に含められる場合を表す．$L$ は実際の契約規模を一意に定める値ではなく，契約バンドリングの許容範囲を表すシナリオパラメータである．

### 3.2.3 期待契約件数の解析式

以上より，橋梁数 $N_k$ の地域における年間期待契約件数は，

$$
f(N_k,L)
=
\mathrm{E}[C(X_k;L)]
=
\sum_{n=0}^{N_k}
\binom{N_k}{n}q^n(1-q)^{N_k-n}
\left\lceil\frac{n}{L}\right\rceil
$$

で与えられる．ただし，$n=0$ の項は0とする．

## 3.3 地域分割最適化モデル

### 3.3.1 記号と決定変数

橋梁の集合を $\mathcal{I}$，$M$ 個の管理エリアの集合を $\mathcal{K}=\{1,\ldots,M\}$ とし，橋梁を $i,j\in\mathcal{I}$，管理エリアを $k\in\mathcal{K}$ で表す．橋梁 $i$ と $j$ の地理的距離を $d_{ij}$ とする．橋梁 $i$ を管理エリア $k$ に割り当てるとき1となる二値変数を $x_{ik}$ とする．

### 3.3.2 目的関数の設計

本研究の地域分割では，年間期待契約件数と管理エリア内の地理的な広がりの両方を考慮する必要がある．地域分割 $x$ の下での年間期待契約件数を

$$
Z(x)=\sum_{k\in\mathcal{K}}f(N_k,L)
$$

とし，同一管理エリアに含まれる橋梁間距離の最大値を

$$
R(x)
=
\max_{k\in\mathcal{K}}
\max_{\substack{i,j\in\mathcal{I}:\,i<j\\x_{ik}=x_{jk}=1}}
d_{ij}
$$

とする．ただし，橋梁を1橋のみ含む管理エリアではエリア内最大距離を0とする．両者を同時に考慮する方法として，まず，次の重み付き和による定式化が考えられる．

$$
\min_x\quad Z(x)+\alpha R(x)
$$

ここで，$\alpha$ は期待契約件数と距離との相対的な重要度を表す重みである．しかし，$Z(x)$ の単位は契約件数，$R(x)$ の単位は距離であるため，$\alpha$ には両者を換算する役割が必要となり，その値を実務的に解釈して設定することは容易ではない．

そこで本研究では，距離を目的関数に含めず，許容可能なエリア内最大距離 $D$ を上限として与える制約型の定式化を採用する．すなわち，

$$
\begin{aligned}
\min_x\quad & Z(x),\\
\mathrm{s.t.}\quad & R(x)\leq D
\end{aligned}
$$

とする．$D$ を複数の値に変化させることで，地理的な広がりの許容範囲と期待契約件数との関係を，単位の異なる指標間の換算を置かずに評価する．距離制約を簡潔に記述するため，距離が $D$ を超える橋梁ペアの集合を

$$
\mathcal{E}_D
=
\{(i,j)\in\mathcal{I}\times\mathcal{I}\mid i<j,\ d_{ij}>D\}
$$

と定義する．

### 3.3.3 定式化

地域分割最適化問題を次のように定式化する．

$$
\begin{aligned}
\min_{x,N}\quad
& Z=\sum_{k\in\mathcal{K}}f(N_k,L), \\
\mathrm{s.t.}\quad
& N_k=\sum_{i\in\mathcal{I}}x_{ik}
&& \forall k\in\mathcal{K}, \\
& \sum_{k\in\mathcal{K}}x_{ik}=1
&& \forall i\in\mathcal{I}, \\
& N_k\geq 1
&& \forall k\in\mathcal{K}, \\
& x_{ik}+x_{jk}\leq 1
&& \forall (i,j)\in\mathcal{E}_D,\ \forall k\in\mathcal{K}, \\
& x_{ik}\in\{0,1\}
&& \forall i\in\mathcal{I},\ \forall k\in\mathcal{K}, \\
& N_k\in\{1,\ldots,|\mathcal{I}|\}
&& \forall k\in\mathcal{K}.
\end{aligned}
$$

目的関数は，管理エリアごとの年間期待契約件数の総和を最小化する．第1制約は管理エリア $k$ に含まれる橋梁数 $N_k$ を定義し，第2制約は各橋梁を一つの管理エリアに割り当てる．第3制約は各管理エリアを非空とする．第4制約は，$\mathcal{E}_D$ に含まれる二橋梁が同一エリアに割り当てられることを禁止し，各エリア内の最大橋梁間距離を $D$ 以下に制限する．

## 3.4 シナリオパラメータと実装

距離上限 $D$ は，管理エリアの地理的な広がりの許容範囲を表すシナリオパラメータとして与える．本研究で用いる $d_{ij}$ は，橋梁の緯度・経度からhaversine公式により算出した地表面上の大円距離であり，道路距離や移動時間を直接表すものではない．

地域数 $M$ も目的関数によって決定せず，外生的なシナリオパラメータとして与える．地域数は，自治体間の連携体制，費用分担，契約主体および意思決定手続と関係するため，期待契約件数のみから一意に決定することはできない．複数の $M$ を比較することで，連携単位の数と契約集約効果との関係を評価する．同時発注上限 $L$ についても複数の値を設定し，バンドリング可能な契約規模が結果に与える影響を確認する．

期待契約件数 $f(N_k,L)$ は $N_k$ に関する非線形関数である．一方，$N_k$ は整数であり，取り得る範囲は有限である．そこで，対象橋梁総数までのすべての整数 $N$ について $f(N,L)$ を事前計算し，隣接する整数点を結ぶ区分線形関数として最適化ソルバーへ与える．決定変数 $N_k$ は整数値のみを取るため，全整数点を節点に用いる場合，各実行可能な $N_k$ において解析式と区分線形表現の値は一致する．

## 3.5 モデルの仮定と適用範囲

本モデルでは，契約集約効果と管理エリアの空間構成との関係を明確にするため，以下を仮定する．

1. 各橋梁は共通の年間補修需要発生確率 $q$ を持ち，補修需要は橋梁間で独立に発生する．
2. 同一年度かつ同一地域内で発生した補修需要は，工種や施工条件にかかわらず，上限 $L$ まで同一契約に含められる．
3. 距離上限 $D$ は地域内の地理的実行可能性を表すが，現場間の道路距離，移動時間および個別の交通条件は扱わない．
4. 地域数 $M$ は外生的に与え，地域間の橋梁数または業務量の均衡は制約しない．また，点として与えられた橋梁のペア距離を制約するため，面的な区域の連続性は明示的に課さない．
5. 施工費用，入札競争性，施工者の供給能力，技術者配置，契約内部の調整費用，複数自治体間の費用分担および意思決定手続はモデル化しない．

したがって，得られる地域分割は，実際の共同発注区域をそのまま決定するものではなく，確率的な補修需要を契約として集約する観点からみた候補を示すものである．実務への適用に際しては，工種の適合性，施工市場，契約制度および自治体間調整等を別途検討する必要がある．

---

> **残課題・確認事項**
> - 第3.1節の期待契約件数の位置付けについて，発注・契約・監督・検査等が契約単位ごとに管理されることを支える国内実務資料の原文を整理する．ただし，契約1件当たりの固定費が一定であるとは仮定しない．
> - 4状態の推定行列から状態III以上を補修状態へ集約する処理は，データ処理・推定手順として4章に記載する．
> - $d_{ij}$ が実装上haversine距離であることをコードと再確認済み．論文では「地表面上の大円距離」と表記する．
> - 本文で採用する $L$，$M$，$D$ の具体値は4章の分析条件へ移し，3章ではパラメータとして定義する．
> - 期待契約件数関数のシミュレーションとの比較は解析式の導出ではなく，旧シミュレーション実装との整合確認として4章または付録に置く．
> - 既存 `main.tex` への反映は，本草稿の議論と引用根拠の確認後に行う．

---

## Source evidence

### 論文化に向けた論点_追加スライド（2026）

- **スライド1–2**: 期待契約件数の削減は補修需要の契約単位集約効果を代理するが，バンドリング効果全体を直接表すものではないと整理されている．発注者・受注者の固定費，間接費，競争性等を期待契約件数単体では評価できない点も明記されている．
- **スライド3–4**: 距離上限 $D$ は，契約集約範囲の拡大と，遠距離・点在化による管理実態との乖離を調整する感度分析パラメータとして整理されている．距離を目的関数に含めると単位換算が必要になるため，制約条件とする方針が示されている．
- **原典**: `references/論文化に向けた論点_追加スライド  -  修復済み.pptx`

### インフラ群のマネジメント最適化に向けた，契約発注のバンドリングと広域連携効果の定量分析

- **スライド21**: 補修前の吸収推移行列 $P$，補修後に状態1へ戻す更新行列 $\widetilde P=T+re_1^{\mathsf T}$，その定常分布 $\pi$，補修確率 $q=\pi r$，二項分布に基づく補修必要数および期待契約件数の解析式が示されている．
- **スライド22**: ハザード率 $\theta_i=\exp(\boldsymbol{\beta}_i^{\mathsf T}\boldsymbol{x})$，期間 $z$ における状態間推移確率，点検前後の健全度と点検間隔を用いた最尤推定というマルコフ劣化ハザードモデルの構成が示されている．
- **原典**: `references/インフラ群のマネジメント最適化に向けた，契約発注のバンドリングと広域連携効果の定量分析.pptx`
